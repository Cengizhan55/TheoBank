spring:
  application:
    name: theo-customer-service

server:
  port: 8200

#management
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always