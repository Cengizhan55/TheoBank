spring:
  application:
    name: theo-payment-service

server:
  port: 8100

#management
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always